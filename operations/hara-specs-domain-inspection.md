# Hara specs production-domain inspection trigger

This temporary branch triggers the trusted `pull_request_target` inspection for `specs.hara-lang.org`. It contains no executable code and will be closed without merge after the domain repair is verified.
